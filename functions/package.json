{
  "name": "safelink-otp-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for SafeLink NSTU: send OTP emails when `email_otps` documents are created.",
  "main": "index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "lint": "eslint .",
    "deploy": "firebase deploy --only functions",
    "start": "firebase emulators:start --only functions,firestore"
  },
  "dependencies": {
    "@sendgrid/mail": "^7.7.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.5.0",
    "twilio": "^5.11.2",
    "@google-cloud/tasks": "^5.0.0"
  }
}
